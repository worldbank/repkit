
* Bad comment
* Worse comment "
/* a comment "
  from the "
  bad place */
/* Weird comment */
// TEST COMMENT

// TEST COMMENT do

// TEST COMMENT [
// TEST COMMENT (
// TEST COMMENT *

do "target-2.do" // missing do-file do "${tf}/target-dontrun.do"

* do "${tf}/target-dontrun.do"
	* do "${tf}/target-dontrun.do"


// do "${tf}/target-dontrun.do"

// do "${tf}/target-dontrun.do"

/* a comment "
	from the "
	do bad place */

	/* do "${tf}/target-dontrun.do" */
  /* do "${tf}/target-dontrun.do" */
/*
do "${tf}/target-dontrun.do"
do nothing
*/

// EOF
