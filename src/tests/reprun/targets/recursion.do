
do "target-2.do" // missing do-file do "${tf}/target-dontrun.do"

* do "${tf}/target-dontrun.do"
	* do "${tf}/target-dontrun.do"


// do "${tf}/target-dontrun.do"

// do "${tf}/target-dontrun.do"

/* a comment "
	from the "
	do bad place */

/*
do "${tf}/target-dontrun.do"
*/
